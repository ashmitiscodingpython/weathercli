[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "weatherclash"
version = "0.0.1"
dependencies = ['requests']

[project.scripts]
todo = "weatherclash.main:main"